# L4: Bond ETF Alpha (Foundation)
base_preset: ../../base/universes/bond_etfs.yaml
volume:
  value_traded_min: 0
trend:
  adx: {enabled: false}
  recommendation: {enabled: false}
  momentum: {enabled: false}
limit: 15
export_metadata:
  symbol: bond_etf_trend
  data_category: strategy_alpha
