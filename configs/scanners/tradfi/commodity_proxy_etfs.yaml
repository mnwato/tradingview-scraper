# L4: Commodity Proxy ETF Basket (Universe Foundation)
base_preset: ../../base/universes/commodity_proxy_etfs.yaml

volume:
  value_traded_min: 0 # Disable hygiene for discovery audit

# Disable trend gating: this scanner is a deterministic basket emitter, not a
# momentum strategy screen.
trend:
  direction: "long"
  logic: "AND"
  recommendation: {enabled: false}
  adx: {enabled: false}
  momentum: {enabled: false}
  confirmation_momentum: {enabled: false}

limit: 25
export_metadata:
  symbol: commodity_proxy_etfs
  data_category: universe_foundation
