# L4: Binance Perp Alpha Trend (Short Only)
base_preset: ../../base/universes/binance_perp_top100.yaml
trend:
  direction: "short"
  logic: "AND"
  adx: {enabled: true, min: 10}
  recommendation: {enabled: true, min: 0.0}
  momentum:
    enabled: true
    horizons: {Perf.W: 0.0}
limit: 50
export_metadata:
  symbol: binance_perp_short_trend
  data_category: strategy_alpha
