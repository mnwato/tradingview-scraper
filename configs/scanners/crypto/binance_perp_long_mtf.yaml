# L4: Binance Perp MTF Trend (Daily + Weekly Confirmation)
# Production scanner for crypto_production profile
# BINANCE-only for institutional execution quality
base_preset: ../../base/universes/binance_perp_top100.yaml

# Primary trend gate (daily fields)
trend:
  direction: "long"
  logic: "AND"
  recommendation: {enabled: true, min: 0.0}
  adx: {enabled: true, min: 10}
  momentum:
    enabled: true
    horizons: {Perf.W: 0.0}

# MTF confirmation screen (weekly)
confirm_screen:
  timeframe: "weekly"
  direction: "long"
  logic: "AND"
  recommendation: {enabled: true, min: 0.0}
  adx: {enabled: true, min: 10}
  momentum: {enabled: true, horizons: {Perf.1M: 0.0}}

limit: 50
export_metadata:
  symbol: binance_perp_long_mtf
  data_category: strategy_alpha
