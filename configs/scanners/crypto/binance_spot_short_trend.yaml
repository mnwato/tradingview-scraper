# L4: Binance Spot Alpha Trend (Short)
base_preset: ../../base/universes/binance_spot_top100.yaml
trend:
  direction: "short" # Logic: Rec <= min, Perf < 0
  logic: "AND"
  adx: {enabled: true, min: 10}
  recommendation: {enabled: true, min: 0.0} # Symmetric to Long (0.0)
  momentum:
    enabled: true
    horizons: {Perf.W: 0.0, Perf.1M: 0.0}
limit: 50
export_metadata:
  symbol: binance_spot_short_trend
  data_category: strategy_alpha
