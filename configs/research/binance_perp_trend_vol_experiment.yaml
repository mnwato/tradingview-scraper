# L4: Binance Perp Trend (Pure Volume Funnel) - EXPERIMENT A
base_preset: ../../base/universes/binance_perp_vol_top50.yaml
trend:
  direction: "long"
  logic: "AND"
  adx: {enabled: true, min: 15}
  recommendation: {enabled: true, min: 0.0}
  momentum:
    enabled: true
    horizons: {Perf.W: 0.0, Perf.1M: 0.5, Perf.3M: 1.0}
limit: 50
export_metadata:
  symbol: binance_perp_trend_vol_funnel
  data_category: strategy_alpha
